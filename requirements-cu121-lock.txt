--extra-index-url https://download.pytorch.org/whl/cu121

# Known working WSL environment:
# Python 3.10.12, CUDA 12.1 PyTorch wheels.
torch==2.5.0+cu121
torchvision==0.20.0+cu121
triton==3.1.0
ninja==1.13.0
packaging==25.0

# RangeViM runtime and tooling dependencies.
einops==0.8.1
scipy==1.15.3
PyYAML==6.0.3
Pillow==12.2.0
tensorboardX==2.6.4
mlflow==3.4.0
prettytable==3.17.0
timm==1.0.12
matplotlib==3.10.9
ruamel.yaml
numpy==2.2.6
opencv-python==4.9.0.80

# nuscenes-devkit is NOT installable alongside this pin set: 1.1.11 requires
# matplotlib<3.6.0, which conflicts with matplotlib==3.10.9 above. It is also
# never imported by the training/eval code -- the nuScenes dataset class reads a
# pre-generated nuscenes_lidar_n_label_data_info.json. Install it in a separate
# environment if you need to regenerate that JSON:
#   pip install nuscenes-devkit==1.1.11   (pulls matplotlib<3.6, Shapely<2.0)

# Install TinyViM selective scan separately so pip can use --no-build-isolation:
# python -m pip install mamba-ssm==2.3.0 --no-build-isolation
